

interface IProps {
    sum: string | number;
    userCurrency: string;
}

const useFormatAmount = ({ sum, userCurrency }: IProps) => {
    // switch (userCurrency) {
    //     case Currency.:
    //         break;

    //     default:
    //         break;
    // }
};

export default useFormatAmount;
