import { createTheme, experimental_sx as sx } from '@mui/material';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#6690FD',
        },
    },
    typography: {
        fontFamily: 'Nunito',
    },
});
